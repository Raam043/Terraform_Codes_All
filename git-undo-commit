--------------------------------------------------------------------
Note: Becareful after this all commits after this will be destroyed
--------------------------------------------------------------------

git log --oneline
git reset --hard HEAD~1   # See your commit which you want to undo
git push -f

or

git checkout commitid
git log --oneline
git reset --hard HEAD~1   # See your commit which you want to undo
git push -f
